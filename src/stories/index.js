import React from 'react';
import { storiesOf, action, linkTo } from '@kadira/storybook';
import BarChart from '../components/BarChart.v1.0.js';

storiesOf('Welcome', module)
  .add('to Storybook', () => (
    <h2>Welcome to a collection of React D3 stories...</h2>
  ));
